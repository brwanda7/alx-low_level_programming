#include "main.h"
#include <string.h>
/**
 * _strcat - concantination fuction
 * @dest : The character to print destination
 * @src : the character that print source
 * Return: return @dest
 */
char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
