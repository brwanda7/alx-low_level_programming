#include "main.h"
#include <string.h>
/**
 * _strcmp - concantination fuction
 * @s1 : The character to print destination
 * @s2 : the character that print source
 * Return: return @l
 */
int _strcmp(char *s1, char *s2)
{
int l;
l = strcmp(s1, s2);
return (l);
}
