#include <stdio.h>

/**
 * main - how to print 0 to 9 using while 
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d\n", i);
		i++;
	}

	return (0);
}
