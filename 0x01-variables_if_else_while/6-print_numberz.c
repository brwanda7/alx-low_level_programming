#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - how to know if number is negative , positive or zero using if
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a = 48;

	while (a < 58)
	{
		putchar(a);
		a++;
	}

	putchar('\n');

	return (0);
}

